# 💡 03. CONDA: Экспорт и импорт

- Сложность:  🟡 Cредняя
- Инструмент: CONDA
- Дата последнего обновления: 2025-11-27

## 🎯 Проблема / Цель

После прохождения этого руководства вы сможете сохранять (экспортировать) настройки своих окружений в файл для передачи коллегам или переноса на другой компьютер, а также восстанавливать (импортировать) их.

## Экспорт окружения (Сохранение)

### 📄 Создание файла конфигурации (YAML)

Экспорт позволяет создать файл, описывающий все установленные библиотеки и их версии. Это необходимо для воспроизводимости вашего проекта.

1. Активируйте окружение, которое хотите сохранить:

```bash
conda activate myenv
```

2. Экспортируйте настройки в файл environment.yml:

```bash
conda env export > environment.yml
```

## Импорт окружения (Восстановление)

### 📥 Создание окружения из файла

Имея файл environment.yml, вы можете развернуть идентичное рабочее пространство одной командой:

```bash
conda env create -f environment.yml
```

Conda автоматически:

1. Создаст окружение (имя обычно берется из первой строки файла .yml).
2. Скачает необходимые версии Python и библиотек.
3. Установит их.

### Обновление окружения из файла

Если вы уже находитесь в окружении и хотите обновить его пакеты согласно файлу:

```bash
conda env update -f environment.yml
```

## 💡 Советы

- Контроль версий: Всегда добавляйте файл environment.yml в ваш Git-репозиторий.
- Pip совместимость: Если ваши коллеги не используют Conda, можно создать стандартный файл требований для pip:

```bash
-    pip list --format=freeze > requirements.txt
   ```

- Проверка: После импорта всегда проверяйте список пакетов командой conda list.

## ↩️ Назад

- ⬅️ Назад к Содержанию ИНСТРУМЕНТ

# 💡 02. CONDA: Виртуальные окружения

- Сложность: 🟢 Новичок
- Инструмент: CONDA
- Дата последнего обновления: 2025-11-27

## 🎯 Проблема / Цель

После прохождения этого руководства вы сможете создавать виртуальные окружения Conda, активировать их и управлять пакетами внутри них.

## Работа с окружениями

### 👷 Создание нового окружения

Для начала работы необходимо создать изолированное окружение. Используйте следующую команду:

 bash
conda create -n myenv python=3.9

- -n myenv: Имя вашего окружения (можно заменить myenv на любое удобное название).
- python=3.9: Указание конкретной версии Python для этого окружения.

### 🟢 Активация и 🔴 Деактивация

После создания окружения необходимо переключиться на него для работы.

```bash
# Активировать созданное окружение
conda activate myenv

# Вернуться в базовое (системное) окружение
conda deactivate
```

## правление пакетами

### 📦 Установка пакетов

Вы можете устанавливать библиотеки по одной, списком или указывая конкретные версии:

```bash
# Установить один пакет
conda install numpy

# Установить несколько пакетов сразу
conda install pandas matplotlib scikit-learn

# Установить конкретную версию пакета
conda install tensorflow=2.13

### 🗑 Удаление пакетов

# Удалить один пакет
conda remove numpy

# Удалить несколько пакетов
conda remove pandas matplotlib
```

## 👀 Мониторинг и обслуживание

### Просмотр информации

Команды для проверки текущего статуса и списка установленного ПО:

```bash
# Список всех доступных окружений
conda env list

# Информация о текущем активном окружении
conda info

# Список установленных пакетов в текущем окружении
conda list
```

### ❌ Удаление окружения

Если окружение больше не нужно, его можно удалить полностью:

```bash
# Основной способ (по имени)
conda env remove -n myenv

# Альтернативный способ
conda remove --name myenv --all
```

## Советы

- Именуйте окружения понятно: используйте названия вроде project-name, data-analysis, web-dev.
- Разделяйте версии: используйте разные версии Python под конкретные задачи.
- Экспортируйте окружения: это важно для воспроизводимости кода на других машинах.
- Соблюдайте чистоту: регулярно удаляйте неиспользуемые окружения.

## ↩️ Назад

- ⬅️ Назад к Содержанию ИНСТРУМЕНТ

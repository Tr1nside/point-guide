# 💡 02. GIT: Базовые команды

* **Инструмент:** [GIT](README.md)
* **Сложность:** 🟢 Новичок
* **Дата последнего обновления:** 2025-11-26

---

## 🎯 Проблема

Как работать с git на базовом уровне?

---

## 1. Инициализация репозитория

### Создать новый репозиторий

Для создания нового репозитория вам необходимо:

* Создать новый каталог
* Перейти в него
* Выполнить:

```bash
git init
```

### Клонировать репозиторий

Создайте рабочую копию локального репозитория, выполнив команду

```bash
git clone /path/to/repository
```

При использовании удаленного сервера ваша команда будет использовать:

```bash
git clone username@host:/path/to/repository
```

## 2. Работа с репозиторием

Ваш локальный репозиторий состоит из трех «деревьев», поддерживаемых git. первый - ваша **Рабочая директория** в котором хранятся сами файлы. Второй - это **Index** (область индексации/staging) который действует как плацдарм и наконец-то **HEAD** который указывает на последний сделанный вами коммит.

![git workflow](img/git_flow.png)

### add & commit

Вы можете добавить изменения в Index с помощью:

```bash
# Добавить конкретный файл в Index
git add <путь к файлу>
# Добавить все измененные файлы в Index
git add .
```

Чтобы зафиксировать эти изменения вы можете использовать:

```bash
git commit -m "Сообщение commit-а"
```

### Отправка изменения на сервер (`push`)

Ваши изменения теперь находятся в HEAD разделе. Чтобы отправить эти изменения на сервер, используйте:

```bash
git push origin <ветка>
```

Если же вы не клонировали существующий репозиторий и хотите подключить свой репозиторий к удаленному серверу, вам необходимо добавить его с помощью:

```bash
git remote add origin <server-url>
```

После этого вы можете выполнить `git push`.

### Загрузка последние изменения с сервера (`pull`)

В ветке которой вы работаете во время внесения вами изменений, другие разработчики могли внести свои изменения.
Если другие разработчики внесли изменения в удаленную ветку, вам нужно загрузить их.

_Важно: Выполняйте pull после того, как вы сделали свой локальный коммит, но перед git push, если ваш push был отклонен._

```bash
git pull origin <ветка>
```

![pull & push](img/git_pull_push.png)

### Просмотр коммитов (`git log`)

Для того чтобы просмотреть историю commit-ов, можно использовать:

```bash
git log
```

Вы увидете в консоли hash, имя и почту автора, дату создания и сообщение commit-а.

## Что дальше?

* [03. GIT: Ветви](03_branches.md)

---

## ↩️ Назад

* [⬅️ Назад к Содержанию GIT](README.md)

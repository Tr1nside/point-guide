# 💡 51. GIT: Ветви - Продвинутые

* **Инструмент:** [GIT](README.md)
* **Сложность:** 🟡 Средний
* **Дата последнего обновления:** 2025-11-28

---

## 🎯 Цель

Вы научитесь продвинутой работе с ветками в Git.

---

## 💥 Стратегии слияния и конфликты (Merging strategies & conflicts)

При объединении веток (`git merge`) Git использует различные стратегии для интеграции изменений.

### 🧩 Стратегии слияния

* **Fast-Forward Merge (По умолчанию):**
  * Происходит, когда целевая ветка (например, `main`) не имела новых коммитов с момента ответвления исходной ветки.
  * Git просто перемещает указатель целевой ветки вперед к последнему коммиту исходной ветки. **Коммит слияния не создается**, история остается линейной.
  * *Когда использовать:* Когда нужна чистая, линейная история, и изменения можно применить напрямую.
* **Three-Way Merge (3-стороннее слияние):**
  * Происходит, когда обе ветки (целевая и исходная) имеют новые коммиты после их общего предка.
  * Git использует три точки: общий предок и последние коммиты обеих веток. Он **создает новый коммит слияния** (Merge Commit).
  * *Когда использовать:* Когда важно сохранить явную запись о том, что произошло слияние веток.

```bash
# Принудительное создание коммита слияния (даже если возможен fast-forward)
git merge --no-ff <branch_name>

# Запрет создания коммита слияния (только fast-forward, иначе отмена)
git merge --ff-only <branch_name>
```

### 💣 Разрешение конфликтов

Конфликт слияния возникает, когда Git не может автоматически объединить изменения, потому что:

1. Одни и те же строки были изменены по-разному в обеих ветках.
2. Файл был удален в одной ветке и изменен в другой.

#### Шаги при конфликте

1. **Обнаружение:** Git останавливает процесс слияния и помечает конфликтующие файлы.
2. **Редактирование:** Вы открываете файл и видите маркеры конфликтов:

    ```python
    <<<<<<< HEAD (или название текущей ветки)
    Текст в текущей ветке, например:
    print("Error in create object")
    =======
    Текст во входящей ветке, например:
    logger.error("Error in create object")
    >>>>>>> <branch_name>
    ```

    Строки <<<<<<<, =======, >>>>>>> — маркеры конфликтов

3. **Разрешение:** Вручную редактируете файл, удаляя маркеры и оставляя нужный код.
4. **Завершение:**

    ```bash
    # 1. Добавляем разрешенные файлы в Index
    git add <conflicted_file>

    # 2. Завершаем слияние
    git commit -m "Merge branch 'feature_X' resolving conflicts"
    ```

---

## 🔄 Перебазирование (Rebasing)

Rebasing (`git rebase`) — это процесс перемещения или комбинирования последовательности коммитов в новую базовую точку. В отличие от слияния (`merge`), которое создает новый коммит слияния, `rebase` переписывает историю, делая ее линейной и чистой

### ❓ Когда стоит использовать Rebase?

* Для очистки истории: Перед слиянием ветки в main вы можете использовать `rebase`, чтобы объединить (`squash`) несколько небольших, промежуточных коммитов в один чистый коммит.
* Для сохранения линейной истории: Если вы хотите интегрировать последние изменения из main в вашу функциональную ветку без создания лишних коммитов слияния.
* Для переноса вашей ветки поверх новой версии main.

### ⚠️ Золотое правило Rebase

* Никогда не делайте `rebase` общедоступной (публичной) историй.
* Rebase создает новые коммиты. Если вы перебазируете ветку, которую уже отправили (`push`) в удаленный репозиторий и с которой работают другие разработчики, это сломает их историю и вызовет конфликты при следующем `push/pull`.

### Базовый rebase

Чтобы перенести вашу текущую ветку (feature_A) поверх самой свежей версии main:

```bash
# 1. Переходим в ветку, которую хотим перебазировать
git checkout feature_A

# 2. Перебазируем ее поверх основной ветки
git rebase main
# Все коммиты из feature_A будут применены после последних коммитов из main.
```

### Интерактивный rebase

Интерактивный `rebase` (`-i` от interactive) — это мощнейший инструмент для переписывания локальной истории перед ее публикацией.
С его помощью можно:

* Объединять (`squash`) несколько коммитов в один.
* Редактировать (`edit`) старые коммиты.
* Переименовывать (`reword`) сообщения старых коммитов.
* Удалять (`drop`) ненужные коммиты.

Синтаксис:

```bash
# Интерактивное перебазирование последних 3 коммитов
git rebase -i HEAD~3
```

После выполнения команды Git откроет текстовый редактор со списком коммитов и инструкциями, где вы можете указать, какое действие применить к каждому коммиту.

## 📜 Отмена изменений и перемещение коммитов (Rewriting History)

Помимо `git rebase -i`, существует ряд команд для управления историей.

### ↩️ Отмена коммитов

| Команда | Назначение | Особенности |
| :--- | :--- | :--- |
| **`git revert <commit>`** | Отменяет изменения определенного коммита, создавая **новый** коммит. | **Безопасный способ** отмены. Не меняет историю. Используется для публичных веток. |
| **`git reset <commit>`** | Сбрасывает текущую ветку на указанный коммит, **удаляя** последующую историю (локально). | **Опасная команда.** Используется только для локальной, неопубликованной истории. |

#### Варианты `git reset`

* **`git reset --soft <commit>`:** Сбрасывает указатель, но оставляет изменения в **Staged** (проиндексированном) состоянии.
* **`git reset --mixed <commit>`** (По умолчанию): Сбрасывает указатель, оставляет изменения в **Working Directory** (рабочей директории), но **не** в Staged.
* **`git reset --hard <commit>`:** **Удаляет все изменения** после коммита в Working Directory и Staged. **Используйте с крайней осторожностью.**

### ✏️ Изменение последнего коммита

Команда `git commit --amend` позволяет изменить последний коммит.

* **Изменение сообщения:**

    ```bash
    git commit --amend -m "Новое сообщение коммита"
    ```

* **Добавление забытых изменений:**

    ```bash
    git add <forgotten_file>
    git commit --amend --no-edit # Добавить файл к последнему коммиту, сохранив сообщение
    ```

---

## ☁️ Работа с удалёнными ветками (Remote Branches)

Удаленные ветки — это ссылки, которые указывают на состояние веток в удаленных репозиториях (например, на GitHub).

### 🏷️ Основные команды

| Команда | Описание |
| :--- | :--- |
| **`git fetch <remote>`** | Загружает новые данные (ветки, теги, коммиты) из удаленного репозитория (**не** сливает их с локальной веткой). |
| **`git pull <remote> <branch>`** | Эквивалентно `git fetch` **и** немедленному `git merge` (или `git rebase`, если настроено) в текущую ветку. |
| **`git push <remote> <branch>`** | Отправляет ваши локальные коммиты в удаленный репозиторий. |
| **`git branch -r`** | Показывает список удаленных веток. |

### 🛠️ Отслеживание удаленных веток (Tracking)

Когда вы создаете локальную ветку из удаленной (`git checkout -b <local_branch> origin/<remote_branch>`), Git автоматически устанавливает связь отслеживания (tracking).

* **Установка связи отслеживания:**

    ```bash
    git push -u origin <branch_name>
    # Ключ -u (или --set-upstream) устанавливает удаленную ветку как "upstream" для локальной.
    ```

* **Удаление удаленной ветки:**

    ```bash
    git push origin --delete <branch_name>
    ```

---

## 🌊 Git Flow / GitHub Flow / GitLab Flow

Это набор правил и соглашений о том, как использовать ветки для управления разработкой.

### 1\. 🐙 GitHub Flow (Самый простой)

* **Используемые ветки:** `main` (или `master`) и **функциональные ветки** (`feature-X`).
* **Принцип:**
  * Ветка `main` всегда должна быть пригодна для развертывания (deployable).
  * Для любой новой работы создается функциональная ветка от `main`.
  * Работа ведется в этой ветке, отправляется (`push`) в удаленный репозиторий.
  * Когда работа готова, создается **Pull Request (PR)**.
  * После ревью и прохождения тестов, ветка сливается в `main` и удаляется.
* *Идеально для:* Непрерывной доставки (Continuous Delivery) и небольших команд/проектов.

### 2\. 🦊 GitLab Flow

* **Используемые ветки:** Добавляет **ветки среды** (`staging`, `production`) к GitHub Flow.
* **Принцип:**
  * Все начинается с функциональной ветки от `main`.
  * После PR, код сливается в `main`.
  * Код из `main` сливается в `staging` для тестирования.
  * Код из `staging` сливается в `production` для выпуска.
* *Идеально для:* Проектов, требующих нескольких этапов развертывания/тестирования.

### 3\. 🌀 Git Flow (Самый сложный)

* **Используемые ветки:** `master`, `develop`, `feature/*`, `release/*`, `hotfix/*`.
* **`master`:** Содержит только код, готовый к продакшену (самый стабильный).
* **`develop`:** Содержит последние интегрированные функции.
* **`feature/*`:** Для разработки новых функций, ответвляются от `develop`.
* **`release/*`:** Для подготовки к новому релизу, ответвляются от `develop`, сливаются в `master` и `develop`.
* **`hotfix/*`:** Для срочных исправлений в продакшене, ответвляются от `master`, сливаются в `master` и `develop`.
* *Идеально для:* Крупных проектов с четко спланированными циклами релизов (например, версионирование v1.0, v2.0).

---

## ↩️ Назад

* [⬅️ Назад к Содержанию GIT](README.md)

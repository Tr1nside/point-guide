# 💡 01. GIT: Установка и настройка

* **Инструмент:** [GIT](README.md)
* **Сложность:** 🟢 Новичок
* **Дата последнего обновления:** 2025-11-26

---

## 🎯 Проблема / Цель

Как установить и настроить git для работы?

---

## 🔽 1. Установка

### **Windows:**

* [Скачать с сайта](https://git-scm.com/install/windows)

### **Linux:**

#### Debian/Ubuntu

(Используется APT)

```bash
sudo apt update
sudo apt install git
```

#### Fedora / RHEL / CentOS

(Используется DNF/YUM)

```bash
# Для современных систем (рекомендовано)
sudo dnf install git
```

_Для старых версий CentOS или RHEL используйте sudo yum install git._

#### Arch-based (Arch / Manjaro)

(Используется Pacman)

```bash
sudo pacman -S git
```

_Рекомендуется использовать `pacman -Syu` git для полного обновления системы перед установкой._

### **MacOS:**

#### Через Homebrew (рекомендовано)

(Требуется установленный Homebrew)

```bash
brew install git
```

#### Через Xcode Command Line Tools

(Простой способ, который часто устанавливает Git)

```bash
git --version
```

_Если Git не установлен, эта команда предложит установить Command Line Developer Tools, в который входит Git._

## ⚙️ 2. Настройка

### **1️⃣ Базовая настройка Git**

#### **Установка имени и email (один раз)**

```bash
git config --global user.name "ВашеИмя"
git config --global user.email "your_email@example.com"
```

#### **Проверить текущие настройки**

```bash
git config --list
```

#### Настройка менеджера учетных данных (credential.helper)

**⚠️ ВАЖНОЕ ПРЕДУПРЕЖДЕНИЕ О БЕЗОПАСНОСТИ:**
Использование _credential.helper=store_ заставляет Git _хранить_ ваши имя пользователя и пароль _в текстовом (незашифрованном) файле на диске_. Это _удобно, но крайне небезопасно_, особенно на общедоступных или рабочих компьютерах. Используйте эту опцию только в том случае, _если вы понимаете и принимаете этот риск_.

**Настройка:**

```bash
git config --global credential.helper store
```

## Что дальше?

Далее можно переходить к [01. GIT: Базовые комманды](02_base_command.md)

---

## ↩️ Назад

* [⬅️ Назад к Содержанию GIT](README.md)
